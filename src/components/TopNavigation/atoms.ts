import { atom } from 'jotai';

export const primaryDropdownAtom = atom<HTMLDivElement | null>(null);

export const secondaryDropdownAtom = atom<HTMLDivElement | null>(null);

export const pillNavigationAtom = atom<HTMLDivElement | null>(null);
