export async function addBookmark(resourceId: string, labId: string, projectId: string) {}
export async function getBookmarkedItems(lab: string, project: string): Promise<string[]> {
  console.log("real one")
  return [];
}
